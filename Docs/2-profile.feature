# Мова: uk
@epic2 @profile
Функціонал: Профіль користувача та соціальна взаємодія
  Як Користувач,
  Я хочу мати можливість переглядати та редагувати свій профіль.
  Також, як Гість або Користувач,
  Я хочу переглядати профілі інших користувачів та підписуватися на них.

  Передумова:
    Дано В базі даних існує користувач "user" з паролем "Password123"
    І В базі даних існує інший користувач "other_user"

# --- Сценарії Перегляду та Редагування Свого Профілю (US-201) ---

  @us-201 @happy-path
  Сценарій: Перегляд свого профілю
    Дано Користувач "user" авторизований в системі
    Коли Користувач переходить на сторінку свого профілю (наприклад, "/profile/user")
    Тоді Він бачить свій нікнейм "user"
    І Він бачить свої дані (ім'я, вік, стать, країна)
    І Він бачить свою статистику (кількість рецензій, кількість підписок)
    І Він бачить кнопку "Редагувати профіль"
    І Він НЕ бачить кнопку "Підписатися"

  @us-201 @happy-path
  Сценарій: Успішне редагування свого профілю
    Дано Користувач "user" авторизований і знаходиться на сторінці редагування профілю
    Коли Він змінює своє "Ім'я" на "Нове Ім'я"
    І Він змінює свій "Вік" на "30"
    І Він натискає кнопку "Зберегти"
    Тоді Дані користувача "user" в базі даних оновлено
    І Його перенаправляє на сторінку профілю "/profile/user"
    І Він бачить повідомлення "Профіль успішно оновлено"
    І Він бачить "Нове Ім'я" та "30" на сторінці

  @us-201 @negative-path
  Сценарій: Невдале редагування (нікнейм вже зайнятий)
    Дано Користувач "user" авторизований і знаходиться на сторінці редагування профілю
    Коли Він змінює свій "Нікнейм" на "other_user"
    І Він натискає кнопку "Зберегти"
    Тоді Дані користувача "user" в базі даних НЕ оновлено
    І Він бачить повідомлення про помилку "Цей нікнейм вже зайнятий"

# --- Сценарії Перегляду Чужого Профілю (US-202) ---

  @us-202 @happy-path
  Сценарій: Користувач переглядає чужий профіль (ще не підписаний)
    Дано Користувач "user" авторизований в системі
    І "user" ще не підписаний на "other_user"
    Коли Користувач "user" переходить на сторінку профілю "/profile/other_user"
    Тоді Він бачить нікнейм "other_user"
    І Він НЕ бачить кнопку "Редагувати профіль"
    І Він бачить кнопку "Підписатися"

# --- Сценарії Підписки/Відписки (US-203) ---

  @us-203 @happy-path
  Сценарій: Користувач підписується на іншого користувача
    Дано Користувач "user" авторизований і знаходиться на сторінці профілю "/profile/other_user"
    І "user" ще не підписаний на "other_user"
    Коли Користувач "user" натискає кнопку "Підписатися"
    Тоді Система створює зв'язок "підписки" (user -> other_user) в базі даних
    І Кнопка змінює назву на "Відписатися"
    І Лічильник "Підписників" у "other_user" збільшується на 1

  @us-203 @happy-path @soft-delete
  Сценарій: Користувач відписується від іншого користувача
    Дано Користувач "user" авторизований і знаходиться на сторінці профілю "/profile/other_user"
    І "user" вже підписаний на "other_user"
    Коли Користувач "user" натискає кнопку "Відписатися"
    Тоді Cистема (м'яко) видаляє зв'язок "підписки" (user -> other_user) в базі даних (встановлює DeletedAt)
    І Кнопка змінює назву на "Підписатися"
    І Лічильник "Підписників" у "other_user" зменшується на 1

# ---
# 
# 🛠️ Завдання для Backend (.Net / EF / Postgres)
#
# 1.  **BaseEntity (Нагадування):**
#     * `Users` успадковує `BaseEntity` (Id (Guid v7), CreatedAt, UpdatedAt, DeletedAt (nullable)).
#     * Налаштувати **Global Query Filter** в EF Core: `.Where(e => e.DeletedAt == null)`.
#
# 2.  **Модель даних: `Follows` (Підписки)**
#     * Ця таблиця також має успадкувати `BaseEntity` для "Soft Delete".
#     * `FollowerId` (Guid, PK, FK до `Users.Id`) - Хто підписався.
#     * `FollowingId` (Guid, PK, FK до `Users.Id`) - На кого підписався.
#     * `CreatedAt`, `UpdatedAt`, `DeletedAt` (nullable).
#
# 3.  **API Ендпоінти (Controller): `ProfileController`**
#     * `GET /api/profiles/{username}`: Отримує дані профілю. Має повертати `isFollowing: true/false` (чи підписаний *поточний* авторизований юзер на цього `{username}`).
#     * `PUT /api/profiles/me`: (Або `/api/users/me`) Оновлює *свій* профіль.
#
# 4.  **API Ендпоінти (Controller): `FollowController`**
#     * `POST /api/profiles/{username}/follow`: Створює запис у `Follows` (або, якщо `DeletedAt` != null, то `DeletedAt = null`).
#     * `DELETE /api/profiles/{username}/follow`: **Soft Delete**. Встановлює `DeletedAt = NOW()` для запису в `Follows`.
#
# ---
#
# 🎨 Завдання для Frontend (Svelte-kit)
#
# 1.  **Динамічний Маршрут: `src/routes/profile/[username]/+page.svelte`**
#     * `+page.ts` (`load` function) має завантажувати `GET /api/profiles/[username]`.
#
# 2.  **Умовний Рендеринг (Conditional Rendering):**
#     * Сторінка має порівнювати `data.profile.username` з `$page.data.user.username`.
#     * **Якщо збігаються:** Рендерити кнопку "Редагувати профіль" (веде на `/profile/edit`).
#     * **Якщо не збігаються:** Рендерити кнопку `FollowButton.svelte`.
#
# 3.  **Компонент: `FollowButton.svelte`**
#     * Приймає `initialIsFollowing: boolean` та `username: string` як `props`.
#     * Керує своїм станом (`isFollowing`).
#     * **При кліку (якщо `!isFollowing`):** Викликає `fetch('/api/profiles/{username}/follow', { method: 'POST' })` і змінює стан/текст.
#     * **При кліку (якщо `isFollowing`):** Викликає `fetch('/api/profiles/{username}/follow', { method: 'DELETE' })` і змінює стан/текст.
#