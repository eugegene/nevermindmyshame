:80 {
    # Логи для зручності
    log

    # 1. Роздача статики (картинки, файли)
    # Якщо запит починається на /uploads - шукаємо в папці /srv/uploads
    handle_path /uploads/* {
        root * /srv/uploads
        file_server
    }

    # 2. Проксі на API
    # Всі інші запити кидаємо на контейнер dev-api, порт 8080
    handle /* {
        reverse_proxy dev-api:8080
    }
}